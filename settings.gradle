pluginManagement {
    repositories {
        gradlePluginPortal()
        maven { url = uri("https://maven.minecraftforge.net") }
        maven { url = uri("https://thedarkcolour.github.io/KotlinForForge/") }
        mavenCentral()
    }
}

rootProject.name = "pillager-campaigns"
